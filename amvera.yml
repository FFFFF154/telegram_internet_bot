---
meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 22
build:
  artifacts:
    build/libs/*.jar: /
  args: spring-boot:repackage
run:
  jarName: main.jar
  persistenceMount: /data
  containerPort: 80
